/*global it, describe, require*/

var chai = require("chai"),
    expect = chai.expect,
    nmdb = require("../libs/nmdb");

describe("NMDB tests", function() {
  it("dummy test", function() {
    expect(true).to.be.true;
  });
});